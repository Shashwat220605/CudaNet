# Contribution checklist

Keep CUDA changes focused, validate correctness against a known reference, and document the hardware/runtime assumptions behind performance results. Never commit credentials or generated local configuration.